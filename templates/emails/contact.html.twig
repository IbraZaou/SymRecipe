<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
	</head>
	<body>
		<h2>Demande de
			{{ contact.fullName }}</h2>

		<p>Email :
			{{ contact.email }}</p>
		<p>Sujet :
			{{ contact.subject }}</p>
		<p>Message :
		</p>

		<div>
			{{ contact.message | raw }}</div>

	</body>
</html>
